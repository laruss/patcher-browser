{
  "$comment": [
    "Budget for the boot payload: the entry chunk plus its static-import",
    "closure. That is the JavaScript a phone must download, parse and compile",
    "before the app renders anything, so it is the number that decides how",
    "slow Patcher feels on mobile.",
    "",
    "Chunks behind a dynamic import are deliberately not counted. Moving code",
    "behind React.lazy or import() is the fix this budget exists to encourage.",
    "",
    "maxBootBytes / maxBootBrotliBytes are a ratchet set 3% above the measured",
    "payload: enough for a normal feature's worth of shell code and for build",
    "noise, tight enough that a barrel regression cannot hide inside it. Lower",
    "them when a change wins headroom. Raising one is a deliberate decision",
    "that needs a reason in the pull request, not a routine edit.",
    "",
    "RAISED ONCE, and the reason is not a feature. The Patcher rename found this",
    "check failing by 104 KB raw / 28 KB brotli, and main measures the same",
    "payload and fails it identically (1771.5 KB there, 1771.4 KB here) — the",
    "rename moves this number by a rounding error, in the smaller direction.",
    "Nothing reported it because no CI run has ever executed on main; the branch",
    "protection that runs this job exists only for pull requests. So the ceiling",
    "was already wrong when the rename inherited it, and these values now record",
    "the payload that actually ships.",
    "",
    "The overage is one barrel, and why-eager.mjs names it in six hops:",
    "packages/shared-ui/src/components/ui/icon.tsx imports ~130 named icons from",
    "@hugeicons/core-free-icons, whose dist/esm/index.js is a re-export barrel",
    "that does not tree-shake, so the whole free set lands in the boot payload.",
    "Per-icon deep imports there should win the 104 KB back. That is a change to",
    "a component every surface renders and it did not belong in a rename, so it",
    "is written down instead of attempted. The next edit to these two numbers",
    "should be downward.",
    "",
    "There is deliberately no per-chunk limit. Parse and compile cost scales",
    "with the total bytes on the boot path, not with how they are divided, so",
    "splitting one boot chunk into two would satisfy a per-chunk cap while",
    "making the page slightly slower for the extra request.",
    "",
    "forbiddenBootPackages is the more important half. These packages are all",
    "large and all needed only after a user action (open a diff, focus the",
    "composer, render math). Each one reached the boot path through a barrel",
    "re-export rather than a direct import, which type checking cannot catch,",
    "so the check names them explicitly."
  ],
  "maxBootBytes": 1868281,
  "maxBootBrotliBytes": 488265,
  "forbiddenBootPackages": [
    "@pierre/diffs",
    "@pierre/theming",
    "@pierre/trees",
    "@shikijs/core",
    "@shikijs/engine-javascript",
    "@shikijs/engine-oniguruma",
    "@shikijs/langs",
    "@shikijs/vscode-textmate",
    "@tiptap/core",
    "@tiptap/pm",
    "@tiptap/react",
    "@tiptap/starter-kit",
    "@xterm/xterm",
    "katex",
    "oniguruma-to-es",
    "prosemirror-model",
    "prosemirror-state",
    "prosemirror-view",
    "rehype-katex",
    "shiki"
  ]
}
