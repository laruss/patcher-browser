{
  "name": "patcher-plugin-omnibox-agent",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "patcher": ">=0.0",
    "patcherPluginSdk": "^1.0.0"
  },
  "patcher": {
    "name": "Omnibox agent",
    "description": "Add agent and site-search suggestions to the browser omnibox.",
    "branding": {
      "icon": "Puzzle"
    },
    "server": "./server.ts",
    "permissions": [
      "omnibox.register",
      "searchEngine.register",
      "threads"
    ]
  },
  "keywords": [
    "patcher-plugin"
  ],
  "scripts": {
    "test": "vitest run --config vitest.config.ts"
  },
  "devDependencies": {
    "@patcher/plugin-sdk": "workspace:*",
    "@types/node": "^22.0.0",
    "typescript": "^6.0.2",
    "typescript-7": "npm:typescript@^7.0.2",
    "vitest": "^4.1.1"
  }
}
