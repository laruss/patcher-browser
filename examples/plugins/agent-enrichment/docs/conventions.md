# Repo conventions

- Use conventional commits: `feat(scope): summary`, `fix(scope): summary`.
- Branch names follow `patcher/<topic>`.
- Keep pull requests focused: one logical change per PR.
- Prefer deleting code over adding configuration.
