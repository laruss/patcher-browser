{
  "name": "patcher-plugin-secrets",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Securely request credentials from a user and reconcile them into dotenv files.",
  "engines": {
    "patcher": ">=0.0"
  },
  "patcher": {
    "name": "Secrets",
    "description": "Securely request credentials from a user and reconcile them into dotenv files.",
    "branding": {
      "icon": "Lock"
    },
    "server": "./src/server.ts",
    "app": "./app.tsx",
    "permissions": [
      "filesystem",
      "threads"
    ]
  },
  "keywords": [
    "patcher-plugin"
  ],
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run --config vitest.config.ts"
  },
  "dependencies": {
    "@patcher/shared-ui": "workspace:*",
    "@hugeicons/core-free-icons": "^4.1.3",
    "@hugeicons/react": "^1.1.6",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@patcher/plugin-sdk": "workspace:*",
    "@types/node": "^22.0.0",
    "@types/react": "^19.0.0",
    "dotenv": "^17.4.1",
    "react": "^19.0.0",
    "typescript": "^6.0.2",
    "vitest": "^4.1.1"
  }
}
