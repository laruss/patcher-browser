{
  "name": "patcher-plugin-ask-user-question",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Let any provider ask the user a multiple-choice question, the way Claude Code's AskUserQuestion does natively.",
  "engines": {
    "patcher": ">=0.0"
  },
  "patcher": {
    "name": "Ask User Question",
    "description": "Let any provider ask the user a multiple-choice question, the way Claude Code's AskUserQuestion does natively.",
    "branding": {
      "icon": "MessageQuestion"
    },
    "server": "./src/server.ts",
    "app": "./app.tsx"
  },
  "keywords": [
    "patcher-plugin"
  ],
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run --config vitest.config.ts"
  },
  "dependencies": {
    "@patcher/shared-ui": "workspace:*",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@patcher/plugin-sdk": "workspace:*",
    "@testing-library/react": "^16.3.2",
    "@types/node": "^22.0.0",
    "@types/react": "^19.0.0",
    "jsdom": "^29.0.1",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "typescript": "^6.0.2",
    "vitest": "^4.1.1"
  }
}
